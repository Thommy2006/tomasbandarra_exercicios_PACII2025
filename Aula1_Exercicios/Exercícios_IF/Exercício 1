# Converter Segundos em Horas, Minutos e Segundos

segundos = int(input("Introduza a hora em segundos: "))

horas = segundos // 3600

minutos = (segundos % 3600) // 60

segundos = segundos % 60

print(f"Horas = {horas}; Minutos = {minutos}; Segundos = {segundos}")